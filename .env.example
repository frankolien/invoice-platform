PORT=3000
APP_ENV=development

# Note: Postgres on 5433 + Redis on 6380 to dodge any local services on default ports
DATABASE_URL=postgres://invoice:invoice@localhost:5433/invoice_platform
REDIS_URL=redis://localhost:6380
APP_URL=http://localhost:3000

JWT_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
ACCESS_TOKEN_TTL_SECS=900
REFRESH_TOKEN_TTL_SECS=604800

# Optional. Leave empty to disable Stripe-dependent endpoints.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional. If set, OTel spans are exported to this OTLP HTTP endpoint.
# Jaeger all-in-one listens on :4318. Leave empty to disable export.
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# Per-org / per-IP rate limit. Default 200. 0 disables.
# Bump high for load tests from a single host: RATE_LIMIT_PER_MIN=100000
RATE_LIMIT_PER_MIN=200

RUST_LOG=info,sqlx=warn
